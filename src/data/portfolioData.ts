import { PortfolioData, NavItem } from "../types";
import { translations } from "./translations";

export const navItems: NavItem[] = translations.th.navItems;
export const portfolioData: PortfolioData = translations.th.portfolioData;
